{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "references": [{ "path": "../common" }] // Required for each package from the same repo
}
